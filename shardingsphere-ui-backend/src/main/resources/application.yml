spring:
  profiles:
    active: pre

server:
  port: 18089

user:
  admin:
    username: admin
    password: admin@bm123

auth:
  api:
    excludes:
      - /api/kms-center/pull/
      - /api/config-center/encrypt/download/
#      - /api/config-center/encrypt/shuffleV2/